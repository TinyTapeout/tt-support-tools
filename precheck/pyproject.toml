[project]
name = "tt-precheck"
requires-python = ">=3.11"
dynamic = ["version"]
dependencies = [
    "gdstk",
    "klayout>=0.30.8,<0.31.0",
    "numpy<2",
    "pytest>=8.4",
    "PyYAML",
    "yowasp-yosys",
]

[tool.setuptools]
packages = []
